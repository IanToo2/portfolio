# AGENTS.md

## 필수 규칙
- 소스 코드에 유니코드 이스케이프(`\uXXXX`)를 사용하지 않는다.
- 한글/영문 텍스트는 실제 문자 그대로 작성한다.
- 인코딩은 UTF-8(BOM 없음)으로 저장한다.

## 코드 수정 기준
- 텍스트 변경 시 인코딩 깨짐이 없는지 확인한다.
- 문자열 치환 후 빌드 또는 실행으로 결과를 검증한다.

## 추가 운영 규칙
- 한글 깨짐 방지:
  텍스트 파일(`src/App.jsx`, `src/styles/global.css` 포함) 수정 전후 UTF-8(BOM 없음) 상태를 확인한다.
- 한글 깨짐 검증:
  문자열 치환 이후 한글 깨짐 패턴이 없는지 검색으로 확인한다.
- 치환 방식 안전:
  단순 문구 수정은 `apply_patch`를 우선 사용한다.
- 멀티라인 수정 안전:
  PowerShell 문자열 치환 시 `` `r`n `` 리터럴이 코드에 들어가지 않도록 here-string 또는 `apply_patch`를 사용한다.
- 빌드 검증 필수:
  코드 수정 후 `npm run build`를 반드시 실행한다.
- 서버 응답 확인:
  개발 서버 실행 중인 경우 `http://localhost:5173` 응답 확인까지 수행한다.
- 원격 저장소 분석 작업:
  분석 목적 작업은 read-only로 수행하고 `commit/push`를 금지한다.
- 임시 클론 관리:
  임시 클론 폴더는 `.tmp_repo_analysis`를 사용하고, 작업 종료 시 반드시 삭제한다.
- 삭제 결과 확인:
  임시 클론 삭제 성공 여부(`DELETED`)를 확인한다.
- 샌드박스 오류 대응:
  `windows sandbox: setup refresh failed` 발생 시 동일 목적 명령을 `require_escalated`로 즉시 재시도한다.
- 우회 금지:
  샌드박스 오류 상황에서 목적이 달라지는 우회 명령을 사용하지 않는다.